<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.andnux.core">
    <!--MIX PUSH-->
    <permission
        android:name="${PACKAGE_NAME}.permission.PUSH_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="${PACKAGE_NAME}.permission.PUSH_RECEIVE" />
</manifest>
